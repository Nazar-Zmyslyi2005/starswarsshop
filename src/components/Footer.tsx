import Box from "@mui/material/Box";
import {styled} from "@mui/material";

export const Footer = () => {
    return (
        <StyledBox>
          dasgdfg
        </StyledBox>
    );
};

const StyledBox = styled(Box)`
    bottom: 0;
    position: absolute;
    width: 100%;
    height: 40px;
    background: green;
`;