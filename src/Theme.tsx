import {useContext} from "react";
import {Context} from "./context/Context.ts";

export const Theme = () => {
    const context = useContext(Context);



    return (
        <div>

        </div>
    );
};